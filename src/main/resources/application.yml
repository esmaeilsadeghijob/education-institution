server:
  port: 7090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cando
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create 