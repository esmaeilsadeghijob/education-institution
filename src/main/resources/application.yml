server:
  port: 5050

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/uni
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update